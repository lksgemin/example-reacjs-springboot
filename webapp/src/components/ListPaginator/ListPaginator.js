import React    from "react";
import template from "./ListPaginator.jsx";

class ListPaginator extends React.Component {
  render() {
    return template.call(this);
  }
}

export default ListPaginator;
